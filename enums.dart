enum PlatformType { staticPlat, moving, disappearing }
enum GameStatus { menu, playing, gameOver }
enum ObstacleType {
  spike,      // gai nhọn
  moving,     // vật cản di chuyển
  deadly,     // chạm là thua
}
